Tabela de Operações de Álgebra Linear e TensorialCategoriaOperaçãoEscopo (2D / 3D / N-D)Algoritmo Mais Eficiente (Acadêmico/Prático)Complexidade TípicaPor que este algoritmo?Básicas & EstruturaisTransposição / Permutação2D, 3D, N-DManipulação de Strides\(O(1)\) (Tempo Constante)Não move dados na memória; apenas altera a ordem dos passos (strides) no cabeçalho do tensor.Básicas & EstruturaisOperações Elemento a Elemento (+, -, *, /)2D, 3D, N-DLoop Paralelo com Zero-Stride Broadcasting\(O(N)\)Se uma dimensão tem tamanho 1, define seu stride como 0. O loop a lê repetidamente sem alocar memória extra.MultiplicaçãoProduto de Matrizes (Dot)2DAlgoritmo de 3 Loops Reordenados (IKJ)\(O(n^3)\)Mudar a ordem tradicional (IJK) para IKJ garante que o acesso à memória seja contíguo, aproveitando o cache da CPU.MultiplicaçãoMatMul em Lote (Batched)3D, N-DContração de Tensores com Strides Iterativos\(O(B \times n^3)\)Itera sobre os eixos de lote (\(B\)) e aplica o algoritmo IKJ nas fatias 2D internas.MultiplicaçãoContração GenéricaN-DEinsum via Reshape + MatMul (BLAS-like)Depende dos eixosIdentifica os eixos contraídos, faz um reshape virtual (\(O(1)\)) para transformar em 2D, multiplica e desfaz o reshape.Sistemas & InversãoResolução de Sistemas (\(Ax = b\))2DDecomposição LU com Pivotamento Parcial (\(PA=LU\))\(O(n^3)\) para decompor; \(O(n^2)\) para resolverO pivotamento evita divisões por zero e instabilidade. Uma vez decomposta, resolve-se qualquer \(b\) muito rápido.Sistemas & InversãoInversão de Matriz2DSubstituição Progressiva/Regressiva via LU\(O(n^3)\)Resolve o sistema \(AX = I\), onde \(I\) é a matriz identidade. É mais estável que o método de Gauss-Jordan puro.Sistemas & InversãoMínimos Quadrados2DDecomposição QR via Reflexões de Householder\(O(m \times n^2)\)Householder usa matrizes de espelhamento ortogonais. É muito mais estável numericamente que o método de Gram-Schmidt.MétricasDeterminante2DProduto da Diagonal da Matriz \(U\) (da LU)\(O(n^3)\) (Custo da LU)Calcular por cofatores é \(O(n!)\) (inviável). Com a LU pronta, o determinante é apenas o produto dos elementos da diagonal de \(U\) multiplicado pelo sinal dos pivots.AvançadasAutovalores e Autovetores2DAlgoritmo QR Iterativo com Deslocamento (Shifts)\(O(n^3)\) (Iterativo)Transforma a matriz em Hessenberg e aplica iterações QR. O "deslocamento" (ex: Rayleigh) acelera drasticamente a convergência.AvançadasValores Singulares (SVD)2DAlgoritmo de Golub-Reinsch (Bidiagonalização + QR)\(O(m \times n^2)\)Reduz a matriz para uma forma bidiagonal usando Householder e depois aplica uma variante do algoritmo QR.Tensores GlobaisReduções por Eixo (Soma, Média, Norma)2D, 3D, N-DIterador de Eixo com Acumulador Contíguo\(O(N)\)Itera sobre o buffer linear pulando os elementos baseados no stride do eixo escolhido para acumular os valores.Dica de Design para a TheFluxComo o seu objetivo é manter a simplicidade acadêmica sem abrir mão da eficiência, a Decomposição LU com Pivotamento Parcial e as Reflexões de Householder (para QR) serão as "joias da coroa" da sua biblioteca. Quase todas as operações complexas de 2D a N-D (solvers, determinantes, inversões, mínimos quadrados) podem ser resolvidas chamando essas duas estruturas base.